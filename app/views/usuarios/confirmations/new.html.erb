<div id="content">
<h2><%= page_title! 'Reenviar instrucciones de confirmación' %></h2>

<%= form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :email, "E-mail" %>
    <%= f.email_field :email %>
  </div>
  
  <div class="actions">
    <%= f.submit "Reenviar instrucciones", :class => 'btn' %>
  </div>
<% end %>

<%= render :partial => "devise/shared/links" %>
</div>