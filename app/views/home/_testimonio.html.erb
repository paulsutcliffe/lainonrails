<% if @testimonio %>
<div class="block">
    <%= image_tag @testimonio.imagen(:thumb) %>
    <h2>TESTIMONIOS</h2>
    <p>
      <%= @testimonio.nombre %>
      <%= truncate(@testimonio.descripcion, :length => 95) %>
    </p>
    <%= link_to 'Leer Más', @testimonio, :class => 'btn read-more' %>
</div>
<% end %>